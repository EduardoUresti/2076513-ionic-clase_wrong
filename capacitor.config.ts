import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'classmiApp-2076513',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
